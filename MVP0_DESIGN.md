# MVP0 Design Archive

This file is intentionally no longer a full implementation spec.

## Current Doc Roles
- [`MVP0_SPEC.md`](MVP0_SPEC.md): implementation source of truth
- [`MVP0_ONE_PAGER.md`](MVP0_ONE_PAGER.md): product framing and success criteria
- [`TASK_BREAKDOWN.md`](TASK_BREAKDOWN.md): milestone plan and execution order

## Why This Was Reduced
The older implementation spec duplicated system rules that now live in `MVP0_SPEC.md`.
Keeping both full versions in the repo creates drift and makes implementation decisions ambiguous.

## Historical Note
This file previously held the draft implementation spec used to shape MVP0.
It is retained only so the old path still exists and clearly redirects to the current documents.
